# Prompt Template (ทั่วไป)

> Template เปล่า สำหรับงานสร้าง/แก้ไขโค้ดแบบไม่ผูกกับระบบเฉพาะ — เติมข้อมูลในส่วน `<...>` ให้ครบ
>
> หมายเหตุ: ถ้างานคือ "สร้างหน้าใหม่ในแอปนี้" ให้ใช้ `note/promt/tabler-theme-page.md` หรือ `note/promt/tabler-theme-page-vertical.md` แทน (มีข้อกำหนดธีมเฉพาะ)

---

## เริ่ม

คุณคือ Senior <TECHNOLOGY> Developer ของโปรเจกต์นี้ (<PROJECT_NAME>)

### เป้าหมาย

<TARGET — เช่น "เพิ่ม endpoint GET /api/users", "แก้บั๊ก X ที่ไฟล์ Y", "สร้าง service Z">

### ข้อมูล

- **ไฟล์/โมดูลที่เกี่ยวข้อง:** <path(s)>
- **ข้อกำหนด/Acceptance criteria:**
  1. <AC1>
  2. <AC2>
  3. <AC3>
- **สิ่งที่ห้ามทำ / ข้อจำกัด:** <เช่น ห้ามแก้ส่วน X, ห้ามเพิ่ม dependency ใหม่>

### ข้อกำหนดบังคับ (ปรับให้เหมาะกับโปรเจกต์)

1. <กติกา 1 เช่น ยึดรูปแบบ/โครงสร้างไฟล์ข้างเคียง>
2. <กติกา 2 เช่น ใช้ pattern เดิมที่มีใน codebase ห้าม reinvent>
3. <กติกา 3 เช่น ต้องมี test / สรุปผล>
4. <กติกา 4 เช่น ห้าม commit ถ้าไม่ได้สั่ง>

### ขั้นตอน

1. ตรวจโครงสร้าง/ตัวอย่างไฟล์ข้างเคียงก่อนเขียนโค้ด
2. <ทำ A>
3. <ทำ B>
4. ตรวจความถูกต้อง: <command เช่น npm run build / npm test>
5. สรุปไฟล์ที่สร้าง/แก้ + สิ่งที่ต้องระวัง

## จบ
